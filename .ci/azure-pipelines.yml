trigger:
  batch: true
  branches:
    include:
      - develop
      - release

resources:
  repositories:
    - repository: templates
      type: git
      name: LFM.Pipelines

stages:
  - stage: Build
    displayName: Build
    jobs:
      - template: build.yml@templates
        parameters:
          applicationName: "lfm-authorization"
